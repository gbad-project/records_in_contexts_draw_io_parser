# Codex Report – 2025-09-14

## Task
- P8T1 – End-to-end example

## Files Reviewed
- `AGENTS.md` – existing description for Phase 8 example workflow.
- `tests/test_draw_io_parser.py` – unit test validating Draw.io parsing against ground-truth Turtle files.
- `tests/test_map_schema_generic.py` – unit test invoking `map_schema` and verifying RML output.
- `tests/scripts/linux/test_add.sh` – shell script running parsing and mapping for description graphs.
- `tests/scripts/linux/test_auth.sh` – shell script running parsing and mapping for authority graphs.
- `tests/scripts/linux/test_rg_1-429.sh` – shell script mapping a specific CSV sample.
- `tests/scripts/linux/test_run.sh` – orchestration script merging generated graphs into `tests/data/store.nq`.
- `parse-add.sh` & `parse-auth.sh` – wrappers around `parse.sh` showing arguments for Draw.io parsing.
- `map_rml.sh` & `map_schema.py` – reference implementation of CSV→RDF mapping and Java RML execution.

## Reasoning
These files together document the legacy Python pipeline from Draw.io and CSV inputs through parsing, preprocessing, mapping,
post‑processing, and merging. They also show how regression tests are executed. Understanding the commands and expected outputs
is crucial for crafting a TypeScript example and for defining parity tests.

## Plan for P8T1
1. Prepare small example inputs (`examples/example.drawio`, `examples/example.csv`) leveraging existing test CSVs.
2. Add `scripts/run-example.sh` and `src/examples/runExample.ts` to orchestrate parsing, mapping, post‑processing, and merging,
   producing `examples/output.nq`.
3. Document regression comparison by running `python tests/test_draw_io_parser.py`, `python tests/test_map_schema_generic.py`,
   and the shell scripts under `tests/scripts/linux/` to ensure behavior matches the Python pipeline.
4. Provide `scripts/test-example.sh` to automate execution of the example and verify that the output file exists.

## Follow-ups
None.
