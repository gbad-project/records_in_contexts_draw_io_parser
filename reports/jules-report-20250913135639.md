# Architect's Report: Task P7T1 Enrichment

**Author:** Jules
**Date:** 2025-09-13
**Timestamp:** 13:56:39

## 1. Introduction

This report details the analysis and enrichment of task `P7T1 – File upload & pipeline orchestration` from `AGENTS.md`. The goal of this effort was to take the high-level description of the UI task and break it down into a detailed, actionable implementation plan for the agent who will build it.

This was achieved by performing a holistic analysis of the entire project, which included:
- A review of the descriptions for all tasks (`P0T1` through `P9T1`) in `AGENTS.md`.
- A thorough analysis of the four original Python scripts (`draw_io_parser.py`, `map_schema.py`, `map_rml.py`, `merge_graphs.py`) that the new TypeScript/React application is designed to replace.

This comprehensive review provided the necessary context to understand not just the individual components of the new pipeline, but also the end-to-end workflow that the UI must successfully orchestrate in the browser.

## 2. Analysis of the End-to-End Workflow

The legacy workflow consists of a chain of Python scripts, each performing a distinct step:

1.  **`draw_io_parser.py`**: Parses a `.drawio` XML file into a Turtle RDF graph. It contains complex logic for interpreting the diagram's geometry and semantics.
2.  **`map_schema.py`**: Takes a schema definition and a source CSV file. It performs extensive, schema-specific preprocessing on the CSV data (splitting/merging columns, etc.) and then generates an RML mapping file that defines how to convert the CSV rows to RDF.
3.  **`map_rml.py`**: Executes the RML mapping using an external Java-based RML engine. It takes the RML file from the previous step and the preprocessed CSV to generate the main RDF graph. It also performs crucial post-processing, such as removing duplicate labels and merging supplementary schema triples.
4.  **`merge_graphs.py`**: Takes multiple RDF graph files and consolidates them into a single N-Quads file. It generates a unique, content-hashed named graph URI for each input file, ensuring that multiple conversion results can coexist in one dataset.

The new TypeScript application must replicate this entire sequence of operations within the browser, orchestrated by the React UI defined in `P7T1`.

## 3. Detailed Plan for P7T1

The following is the detailed breakdown of subtasks that will be inserted into `AGENTS.md` to enrich the `P7T1` task description.

---

## 4. Addendum: Post-Submission Architectural Correction

Following a review, a flaw was identified in the initial breakdown of `P7T1`. The initial plan did not account for the user-configurable nature of the `P1T3 – Prefix Expansion Utility`.

The `createPrefixExpander` function in `P1T3` accepts a `prefixMap` as an argument. This `prefixMap` is intended to be supplied by the user. The most logical place for this user interaction is the UI (`P7T1`).

The corrected architecture, which will be reflected in the updated `AGENTS.md`, is as follows:

1.  **UI Handles Configuration:** The UI (`P7T1`) will be responsible for gathering the `prefixMap` from the user (e.g., via a text input or file upload).
2.  **Orchestrator Creates Dependency:** The UI's orchestration logic will call `createPrefixExpander(prefixMap)` to create a configured `expand` function.
3.  **Dependency is Injected:** The orchestrator will then pass this `expand` function as a parameter to the mapping engine (`P4T2`).

This approach correctly separates concerns, making the UI responsible for configuration and the mapping engine responsible for execution, using the dependencies it is provided. The updated `P7T1` task description will reflect this improved design.
